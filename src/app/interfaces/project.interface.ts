export interface Project {
  submissionId: number;
  competitionId: number;
  userId: number;
  projectTitle: string;
  description: string;
  githubLink: string;
  submissionDate: string;
  status: string;
  rank: number;
}
